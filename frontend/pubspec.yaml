name: finance_app
description: Finance System - Gestão Financeira Pessoal

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  provider: ^6.1.2
  fl_chart: ^1.2.0
  intl: ^0.20.2
  path_provider: ^2.1.3
  share_plus: ^10.0.0
  sign_in_with_apple: ^6.0.0
  local_auth: ^2.3.0
  crypto: ^3.0.3
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  firebase_crashlytics: ^4.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true